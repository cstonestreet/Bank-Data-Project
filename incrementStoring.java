/**
 * this class stores two arrayLists, one for storing the recordIDs, and one for storing the increment values of each recordID
 */
public class incrementStoring {

    MUArray<String> recordIDS = new MUArray<>();
    MUArray<Integer> increment = new MUArray<>();
}